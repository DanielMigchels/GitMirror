apiVersion: v1
kind: Service
metadata:
  name: gitmirror-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: gitmirror-service
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 32112
  selector:
    app: gitmirror-deployment